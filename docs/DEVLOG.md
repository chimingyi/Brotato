# AI 游戏开发日志

这里记录每一步做了什么、为什么这样做、怎样验证，以及下一步准备做什么。

## 2026-08-21｜第 1 步：让角色出现在屏幕上并移动

### 本步目标

先完成最小的互动：打开网页后，玩家能够控制角色在场地中移动。

### 做了什么

- 选择原生 HTML、CSS 和 JavaScript，不依赖游戏引擎。
- 用 Canvas 创建 16:9 的游戏场地。
- 用简单图形画出原创的薯星探险员，不使用外部素材。
- 加入 `WASD` 和方向键移动。
- 使用“每秒速度 × 两帧之间的时间”计算位移，让不同性能的电脑上速度接近一致。
- 限制角色位置，避免跑出屏幕。
- 加入开始界面、计时、生命条和空格暂停。

### 为什么从移动开始

游戏开发最重要的办法是把大目标拆成能马上验证的小目标。角色移动是玩家与游戏的第一次互动，也是后面敌人追踪、碰撞和拾取系统的基础。

### 怎样验证

1. 启动本地网页服务器。
2. 打开游戏，点击“开始冒险”。
3. 分别按 `WASD` 和方向键。
4. 检查斜向移动是否不会比横向移动更快。
5. 走到四条边，检查角色是否会留在场地内。
6. 按空格，检查计时和移动是否暂停。

### 遇到的问题

- 本地仓库还没有设置 GitHub 远程地址。
- 通过 HTTPS 读取远程仓库时需要 GitHub 登录，因此暂时不推送。

### 下一步

加入第一种敌人：敌人从场地外出现，并自动追踪玩家。

## 2026-08-21｜第 2 步：加入敌人与自动战斗

### 本步目标

建立最小战斗循环：敌人出现并追踪玩家，玩家自动攻击，双方都能造成伤害。

### 做了什么

- 敌人会随机从场地四周出现，并朝玩家移动。
- 生存时间越长，敌人的移动速度和生命值会缓慢增加。
- 玩家会寻找最近的敌人，自动发射种子子弹。
- 子弹与敌人使用圆形距离检测完成碰撞。
- 敌人死亡后增加击败数量，并产生简单粒子效果。
- 敌人碰到玩家会扣除生命；短暂无敌时间可避免一帧内连续扣血。
- 生命归零后显示本局时间和击败数，可以重新开始。
- 页面失去焦点时自动暂停，避免玩家切换窗口后被敌人偷袭。

### 这一步学到的概念

- **数组**：用来保存当前场上的所有敌人、子弹和粒子。
- **游戏循环**：每一帧更新位置、检测碰撞，然后绘制新画面。
- **状态**：开始、暂停和失败是三种不同的游戏状态。
- **难度曲线**：根据生存时间逐渐调整敌人强度，而不是突然变难。

### 怎样验证

1. 开始游戏后等待约 2 秒，确认敌人从边缘出现。
2. 不按攻击键，确认玩家会自动朝最近敌人发射子弹。
3. 观察击败数字是否增加。
4. 故意不移动，确认生命会下降并最终显示失败面板。
5. 点击“再试一次”，确认时间、生命和击败数全部重置。

### 下一步

让敌人掉落能量，加入经验值、升级和“三选一”强化。

## 2026-08-21｜第 3 步：经验与三选一升级

### 本步目标

让战斗产生奖励，玩家可以在一局中逐渐形成自己的成长路线。

### 做了什么

- 敌人死亡时掉落青色能量晶体。
- 晶体进入拾取范围后会加速飞向玩家，远处的晶体也会缓慢靠近，避免永久留在场上。
- 加入经验条、等级和逐级增加的升级需求。
- 升级时暂停战斗，并随机显示三个强化选项。
- 第一批强化包括：伤害、攻速、移速、生命、拾取范围和多重射击。
- 重新开始时会重置所有升级，保证每一局都从相同起点开始。

### 这一步学到的概念

- **数据驱动**：所有强化用统一的数据结构保存，增加新强化时不需要重写界面。
- **随机抽取**：从可用强化中洗牌后取三个，并排除已经达到上限的多重射击。
- **暂停状态**：选择强化时停止战斗，选完后继续，而不是另开一个游戏循环。

### 怎样验证

1. 击败敌人，确认地面出现青色菱形晶体。
2. 靠近晶体，确认它会被吸向角色并增加经验。
3. 经验条满后，确认战斗暂停并出现三个不同选项。
4. 选择强化，观察对应数值或战斗效果是否发生变化。
5. 重新开始，确认等级、经验和强化都恢复初始状态。

### 下一步

加入不同类型的敌人和另外两种武器，让每局的战斗变化更丰富。

## 2026-08-21｜第 4 步：敌人种类与武器构筑

### 本步目标

避免战斗一直重复同一种情况，让玩家需要根据敌人和随机强化调整选择。

### 做了什么

- 保留基础孢子敌人，15 秒后加入速度快但生命低的疾行虫。
- 35 秒后加入移动慢、生命高、碰撞伤害高的岩壳怪。
- 不同敌人使用不同的形状、颜色、体型和属性。
- 受伤敌人头顶会显示小生命条，重装敌人更容易看出战斗进度。
- 新增“守护叶片”：叶片环绕玩家，对靠近的敌人持续造成伤害。
- 新增“星芒爆发”：每隔几秒向四周发射一圈星芒，升级后弹量和伤害增加。
- 三种武器共用已有的碰撞和粒子系统，减少重复代码。

### 这一步学到的概念

- **敌人模板**：类型决定基础属性，时间难度再统一放大属性。
- **复用函数**：所有武器都通过相同的击败函数掉落经验和增加击败数。
- **冷却时间**：环绕叶片给每个敌人设置命中冷却，防止每帧都计算一次伤害。

### 怎样验证

1. 生存超过 15 秒，确认橙色小型疾行虫出现。
2. 生存超过 35 秒，确认灰色大型岩壳怪出现。
3. 升级时选择守护叶片，靠近敌人观察叶片造成伤害。
4. 选择星芒爆发，观察周期性的环形弹幕。
5. 重新开始，确认两种额外武器都需要重新获取。

### 下一步

补齐手机触控、声音反馈、胜利条件、最高纪录和完整结算界面。

## 2026-08-21｜第 5 步：手机、音效、结算与最终测试

### 本步目标

把核心玩法整理成一款有开始、有过程、有结束，并且电脑和手机都能玩的第一版游戏。

### 做了什么

- 加入手机虚拟摇杆，拖动控制方向，松手后自动回到中心。
- 用 Web Audio 现场生成射击、拾取、升级、受伤、失败和胜利音效，不需要下载音频素材。
- 加入音效开关，并遵守浏览器“用户点击后才能播放声音”的限制。
- 一局目标设为 5 分钟，坚持到最后会显示胜利结算。
- 失败和胜利都会记录生存时间与击败数，并可以重新开始。
- 使用浏览器本地存储保存最高生存纪录。
- 手机开始界面会隐藏摇杆，开局后再显示，避免挡住按钮。

### 实际测试结果

- 桌面首次运行 8 秒：击败 5 个敌人，生命 100，控制台 0 个错误。
- 成长测试 22 秒：升到 2 级，出现 3 个不同强化，选择后战斗正常继续。
- 内容测试 39 秒：疾行虫和岩壳怪均已出现，等级 4，击败 33 个敌人。
- 失败测试 45 秒：正确显示结算面板和“再试一次”按钮。
- 手机测试尺寸：390 × 844；页面横向滚动宽度等于屏幕宽度，摇杆可见并能接收拖动。
- JavaScript 语法检查通过，HTML、CSS、JavaScript 均能由本地服务器正常读取。

### 测试发现并修复的问题

1. 生命值被最后一次伤害扣到 0 以下时，界面曾显示负数；现在最低显示为 0。
2. 手机开始页面的按钮曾与经验条重叠；现在隐藏冗长说明并压缩布局。
3. 手机摇杆最初在开始前出现；现在只在正式开局后显示。
4. 一次 42 秒的自动化检查超过测试工具单次时间限制；之后拆成短测试，确认这是测试连接超时，不是游戏崩溃。

### 第一版结论

第一版核心功能已经完成并通过本地测试。剩余发布工作是登录 GitHub、推送仓库并开启 GitHub Pages。

## 2026-08-21｜发布尝试记录

- 已在本地创建 `main` 分支和第一版 Git 提交。
- 已关联远程仓库 `https://github.com/SuperPig0722/Brotato.git`。
- HTTPS 推送需要 GitHub 登录，当前终端尚未保存该账号凭据。
- 电脑已有的 SSH 密钥登录为另一个账号 `chimingyi`，该账号没有目标仓库权限，因此没有使用错误账号发布。
- 下一步需要使用 `SuperPig0722` 登录，或把现有 SSH 账号添加为仓库协作者，然后再次推送。

### 仓库迁移结果

- 原仓库因账号权限不一致无法推送。
- 新建仓库：`git@github.com:chimingyi/Brotato.git`。
- 已将本地 `origin` 切换到新仓库，并成功推送完整的 `main` 分支。
- 本地与远程分支均指向提交 `f297f53`，Git 核对结果一致。
- 新仓库目前为私有仓库；是否改为公开并开启 GitHub Pages，需要仓库所有者确认。

## 2026-08-21｜发布前完成度审计

早期测试靠等待真实战斗发生，能够证明基本玩法，但没有直接走完 5 分钟胜利，也没有稳定抽到两种特殊武器。为避免用“应该能运行”代替证据，本次增加了只在本地地址生效的快速测试参数。

### 本地测试模式

- 必须同时满足：网页运行在 `localhost`、`127.0.0.1` 或 `::1`，并且网址带有 `test=1`。
- 可以临时缩短回合、加快第一次升级、固定强化选项或快速触发失败。
- GitHub Pages 使用公开域名，因此这些参数在正式发布版中不会生效，正式回合仍为 5 分钟。

### 直接验证结果

- 胜利：2 秒测试局显示“你守住了薯星！”，时间、总结和最高纪录均为 `00:02`。
- 胜利后重开：时间 `00:00`、生命 `100 / 100`、等级 1、击败 0、经验 0，武器只剩种子发射器。
- 守护叶片：选择后武器栏显示“守护叶片 ×1”，画面中出现环绕叶片。
- 星芒爆发：选择后武器栏显示“星芒 Lv.1”，画面中出现紫色星芒弹丸。
- 失败：1 秒测试局显示“探险员倒下了”，生命最低为 `0 / 100`，不会出现负数。
- 失败后重开：生命、时间、等级、击败和面板状态全部恢复初始值。
- 所有场景的浏览器错误和警告数量均为 0。
- 已保存真实浏览器运行截图 `docs/screenshots/gameplay.jpg`。

完整复测方法见 `docs/TESTING.md`。

## 2026-08-21｜配置 GitHub Pages 自动发布

- 仓库所有者已手动把 `chimingyi/Brotato` 从私有改为公开。
- 匿名访问仓库页面和 `index.html` 原始文件均成功，公开状态已验证。
- 新增 `.github/workflows/deploy-pages.yml`。
- 工作流只拥有读取代码、写入 Pages 和获取部署身份令牌所需的最小权限。
- 每次推送 `main` 都会自动打包项目根目录并部署；也可以在 Actions 页面手动运行。
- 预期游戏地址：<https://chimingyi.github.io/Brotato/>。

### 实际发布与线上验收

- 首次 Actions 尝试失败，原因是 Actions 身份没有权限替管理员首次创建 Pages 站点；这不是游戏代码错误。
- 创建 `gh-pages` 发布分支后，GitHub 自动建立了 Pages 站点。
- 公开网址：<https://chimingyi.github.io/Brotato/>。
- 线上页面标题为“薯星幸存者”，开始界面、信息栏、Canvas 和脚本资源均加载成功。
- 点击“开始冒险”后实际运行 8 秒：计时为 `00:08`，自动击败 5 个敌人，生命为 `100 / 100`。
- 线上浏览器错误和警告数量为 0。
- 至此，第一版已经完成“本地开发 → Git 提交 → GitHub 仓库 → 公开网址 → 线上试玩”的完整流程。

### 自动更新方式调整

- Pages 首次创建后采用 `gh-pages` 分支作为发布源。
- 官方 `deploy-pages` 工作流从 `main` 发布时，被环境保护规则拒绝，因为当前只允许 `gh-pages` 发布。
- 保留环境保护规则，改为由 Actions 自动把 `main` 的同一提交同步到 `gh-pages`。
- 添加 `.nojekyll`，确保 GitHub Pages 原样提供 HTML、CSS、JavaScript 和文档文件。
- 以后仍然只需在 `main` 开发；发布分支由工作流维护，不需要手动修改。

### 自动更新最终验证

- “同步游戏到 GitHub Pages”工作流运行成功。
- 远程 `main` 与 `gh-pages` 均指向提交 `fb62631`，证明发布分支没有漏掉更新。
- 线上 `README.md` 已出现最新的“在线游玩”链接，证明新提交确实到达 Pages，不只是工作流表面成功。
- 路线图所有项目均已完成，第一版标记为 `v1.0.0`。

## 2026-08-21｜v2 第 1 步：从生存原型升级为完整构筑游戏

### 为什么需要重构

v1 的核心是“一张场地坚持 5 分钟”，能够证明移动、自动攻击、经验和升级可行，但没有短波次与商店，因此还没有形成这类游戏最重要的“战斗表现影响经济，经济选择影响下一波”的循环。

### 新目标

- 保持原创名称、美术、文字和数值。
- 从玩法系统层面对标完整的短波次自动战斗构筑体验。
- 使用独立开发分支 `codex/gameplay-parity-v2`，避免未完成的重构影响线上 v1。
- 先完成 M1 的一整套可玩闭环，再扩充武器、道具、角色和敌人数量。

### 本步产物

- 新增 `docs/GAMEPLAY_PARITY_V2.md`，列出完整流程、系统边界和逐项验收清单。
- 确定五个里程碑：完整波次循环、商店装备、属性内容、20 波难度、局外解锁。

## 2026-08-21｜v2 第 2 步：完成第一波玩法闭环

### 本步目标

先不追求大量内容，证明一局游戏可以可靠地在“选择、战斗、成长、消费、继续战斗”之间切换。

### 做了什么

- 把旧的单局生存流程重构为 `loadout`、`wave`、`transition`、`upgrade`、`shop` 和 `gameover` 六个阶段。
- 加入 3 名原创探险员和角色专属初始武器选择。
- 加入 20 波数据入口、独立波次倒计时、材料、经验、收获收益和波末自动回收。
- 加入升级四选一、四货架商店、购买、刷新、锁定和价格成长。
- 加入六个武器槽、四个品质、同名同品质自动合成和出售返还材料。
- 把角色、武器、道具、敌人、属性和波次配置移到 `src/data.js`，方便以后批量扩充内容。
- 增加只在本地测试模式生效的起始材料和经验参数，使商店功能可以稳定复测。

### 实际浏览器测试结果

- 完整走通：嫩芽先锋 → 种子发射器 → 第 1 波 → 升级四选一 → 商店 → 第 2 波。
- 第 1 波正常结算击败数、材料和收获，第 2 波 HUD 正确显示 `2 / 20`。
- 锁定“根须短棍”后刷新，其他三个货架发生变化，锁定货物保持不变；进入下一波并再次结算后仍然保留。
- 购买一把普通“种子发射器”后，原武器自动升级为精良品质，武器槽仍为 `1 / 6`。
- 购买“透镜种荚”后，远程伤害从 `+0` 变为 `+2`，武器显示伤害同步变化。
- 购买第二把武器后出现出售按钮；出售成功，武器槽回到 `1 / 6`，材料从 56 增加到 63。
- 桌面商店布局和 390 × 844 手机选择界面均已检查，内容可以滚动，没有脚本崩溃。
- JavaScript 语法检查、`git diff --check` 和浏览器控制台检查均通过；错误和警告为 0。

### 这一步学到的概念

- **状态机**：用明确阶段控制哪些界面和规则正在运行，避免战斗、升级和商店同时响应。
- **数据驱动**：内容和规则分开，后续增加武器或道具时主要添加配置，不需要复制整段战斗代码。
- **可重复测试**：本地测试参数能稳定制造材料和升级机会，避免靠随机结果判断功能是否正确。

### 下一步

进入 M2，补上严格的合成规则、武器类别套装加成、六槽满载边界测试和更清晰的武器详情，然后扩充第一批武器与道具。

## 2026-08-21｜v2 第 3 步：完成六槽、连续合成与类别加成

### 本步目标

让武器背包不仅能装多把武器，还要有明确的容量规则、品质成长和围绕类别搭配的构筑理由。

### 做了什么

- 把合成改为连续规则：新武器与同名同品质武器合成后，如果又遇到同名同品质，会继续向上合成。
- 六个武器槽满载后，不能合成的新武器会自动禁止购买；可合成武器仍然允许购买。
- 新增植物、精准、钝击、元素、充能和灵巧六类套装加成。
- 类别数量达到 2 到 6 把时逐级增强，并实时影响收获、远程伤害、护甲、元素伤害、攻击速度或闪避。
- 商店武器栏新增类别徽章，未激活时显示距离下一档的数量，激活后显示实际属性收益。
- 新增本地固定武器、固定品质和品质序列参数，用来稳定复测随机商店的边界情况。

### 实际浏览器测试结果

- 购买 5 把额外武器后正确显示 `6 / 6`，剩余三件传说武器的购买按钮全部禁用。
- 5 把植物武器激活 `+15 收获`，属性面板从角色基础 `+5` 变成 `+20`。
- 3 把灵巧武器激活 `+6 闪避`；2 把精准武器激活 `+2 远程伤害`，武器伤害同步变化。
- 背包同时存在普通与精良种子发射器时，再购买普通种子发射器，最终连续合成为一把稀有武器，槽位回到 `1 / 6`。
- 合成后的稀有武器伤害为 25，说明品质倍率和升级获得的远程伤害均参与计算。
- JavaScript 语法、补丁格式和浏览器控制台检查通过，错误和警告为 0。

### 下一步

进入 M3：扩充武器、道具和角色数据，并补充燃烧、弹射、工程装置等目前还没有的攻击机制。

## 2026-08-21｜v2 第 4 步：完成角色、武器、道具与派生机制框架

### 本步目标

把少量演示内容扩充成可以支撑大量构筑组合的数据规模，并确保新武器在玩法上有实际差异。

### 做了什么

- 探险员从 3 名扩充到 12 名，每名角色都有加成、代价和三把推荐初始武器。
- 武器从 4 把扩充到 24 把，覆盖近战、远程、元素与工程缩放。
- 道具从 10 件扩充到 80 件，覆盖 18 项基础属性以及 8 件带正负取舍的混合道具。
- 新增工程、爆破和利刃三种武器类别及对应套装加成。
- 新增多弹丸与散射、范围爆炸、持续燃烧、移动减速和目标弹射机制。
- 燃烧和减速会显示橙色、蓝色状态环；燃烧可以独立击败敌人并正常掉落材料。
- 增加只在本地测试模式出现的只读运行计数，避免只凭画面猜测高级机制是否真的触发。

### 数据检查结果

- 角色数量：12。
- 武器数量：24。
- 道具数量：80。
- 所有角色的初始武器 ID 都能在武器表中找到。
- 所有道具修改的属性都存在于基础属性表中。
- 角色、武器和道具 ID 没有重复。

### 浏览器实测结果

- 余烬法球：11 次攻击触发 10 次燃烧。
- 熔核果实：6 次攻击触发 4 次爆炸和 4 次燃烧，测试时有 1 个敌人正在燃烧。
- 月芽长弓：第 10 波 10 次攻击触发 3 次弹射。
- 霜囊投射器：第 10 波 11 次攻击触发 10 次减速，测试时有 1 个敌人处于减速状态。
- 花粉迸射器：7 次攻击生成 21 发弹丸，符合每次三发的设计。
- 齿轮培育员的哨戒种核显示 18 伤害，高于基础 12，工程属性缩放正确生效。
- 浏览器测试期间没有脚本错误或警告。

### 下一步

进入 M4：把敌人扩充到普通、远程、冲锋、召唤、治疗和强化等行为，并加入精英、首领、危险等级与完整 20 波配置。

## 2026-08-21｜v2 第 5 步：完成敌人生态、精英首领与危险等级

### 本步目标

让 20 波不再只是相同敌人的数值增长，而是随着波次持续引入新行为，并在关键波次形成精英和首领战。

### 做了什么

- 普通敌人扩充到 16 种，随波次逐步解锁并从最近 7 类中生成。
- 新增远程射击、狙击、冲锋、治疗、强化光环、召唤、分裂、接触爆炸、绕行、传送和固定炮台行为。
- 新增 4 种原创精英：荆冠斗士、雷芽祭司、育巢守望者、铁木巨像。
- 新增 3 种原创首领：万巢母体、风暴星核、岩根泰坦。
- 第 6、9、13、17 波固定出现不同精英，第 20 波根据危险等级选择首领。
- 加入 D0 萌芽到 D4 星蚀五个危险等级，同时提高生命、伤害、速度和生成密度。
- 角色选择页面加入危险等级选择和说明；战斗 HUD、商店和结算均显示当前危险等级。
- 精英和首领拥有固定在画面上方的名称与生命条。
- 新增敌方弹丸系统、敌方护甲和首领/精英视觉外圈。

### 数据检查结果

- 普通敌人 16 种、精英 4 种、首领 3 种、危险等级 5 档。
- 1 到 20 波都存在有效的敌人池、持续时间和生成间隔。
- D4 的生命、伤害、速度倍率和生成密度均高于 D0。

### 浏览器实测结果

- D4 第 6 波生成荆冠斗士：生命 1206，触发 1 次冲锋；同场远程敌人成功生成敌方弹丸。
- 第 13 波生成育巢守望者：4.2 秒内召唤 10 个单位。
- 第 17 波生成铁木巨像：1.4 秒内触发治疗脉冲。
- D0 万巢母体：触发 6 次召唤。
- D1 风暴星核：2.2 秒内生成 15 发敌方弹丸。
- D2 岩根泰坦：触发冲锋，测试生命为 8122。
- D4 第 20 波快速测试正确显示“薯星防线守住了”和危险等级结算。
- 所有浏览器测试的错误和警告数量均为 0。

### 下一步

进入 M5：加入本地存档、危险等级解锁、角色与内容图鉴、设置选项、重置存档和完整发布验收。

## 2026-08-21｜v2 第 6 步：完成局外成长、完整规则与辅助设置

### 本步目标

让每次远征都能留下进度，并补齐完整构筑游戏需要的可破坏物、治疗消耗品、高品质升级和道具限制。

### 做了什么

- 新增 v2 独立存档，记录远征次数、胜场、最高波次、危险等级、角色、武器、道具和敌人发现进度。
- 初始开放 3 名角色、8 把武器和 24 件道具；危险等级从 D0 开始逐级解锁。
- 加入图鉴、新解锁提示、新手指南、音量、屏幕震动、伤害数字设置和两步确认重置存档。
- 正式进度保存到浏览器；本地 `test=1` 使用隔离进度，快速测试不会污染玩家存档。
- 每波生成原创树木，武器在没有敌人时也会攻击树木；破坏后按幸运概率掉落治疗果实。
- 治疗果实在玩家靠近时自动吸附，受伤时拾取可恢复生命并显示绿色回血数字。
- 升级加入普通、精良、稀有、传说四档品质，等级、波次和幸运会提高高品质概率。
- 前两波商店至少提供两把武器，之后至少一把，并倾向玩家已有的武器类别。
- 道具加入唯一、最多持有数量和互斥组规则；主要概率与速度属性加入明确上限或下限。

### 怎样验证

1. 使用初始锁定测试档检查角色、内容和危险等级数量。
2. 快速完成 D0 第 20 波，检查 D1、角色、武器、道具和统计是否更新。
3. 修改设置并刷新页面，检查设置是否保留。
4. 固定生成一棵低生命树，把玩家生命设为 50，观察攻击、掉落、吸附和回血。
5. 强制传说升级，检查四张升级卡的品质和放大后的数值。
6. 结束第 1 波，统计商店四个货架中的武器数量。
7. 分别购买唯一道具和尝试购买互斥道具，检查按钮是否禁用。

### 实际测试结果

- 初始图鉴正确显示 3/12 名角色、8/24 把武器、24/80 件道具、0/23 个敌人记录。
- D0 通关后解锁 D1，并显示远征 1 次、最高第 20 波、通关 1 次和新增内容。
- 音量从 70% 改为 35%、关闭震动和伤害数字后刷新仍保持；随后已恢复默认设置。
- 种子发射器攻击 1 次后摧毁 1 棵树，掉落并拾取 1 个治疗果实，生命从 50 恢复到 58。
- 强制品质测试出现 4 个传说升级：核心伤害属性为 `+5`，幸运为 `+17`。
- 第 1 波商店出现 2 把武器和 2 件道具，满足前期武器保底。
- 购买玻璃幼芽后同名商品全部禁用；持有玻璃幼芽时，互斥的铸铁田靴不可购买。
- 熔核果实的伤害数字和屏幕震动计数均为 2，证明两个设置实际控制战斗反馈。
- `src/game.js`、`src/data.js`、`src/storage.js` 语法检查和 `git diff --check` 均通过。

### 下一步

进行发布前平衡审计和完整回归测试。只有通过后才把 v2 合并到 `main` 并更新线上版本。

## 2026-08-21｜v2 第 7 步：发布前平衡与 20 波回归

### 做了什么

- 检查 D0-D4 的持续时间、生成间隔、预计敌人数、生命、伤害和速度倍率。
- 比较 24 把武器的基础每秒伤害，并结合射程、范围攻击、穿透、弹射和价格判断差异是否合理。
- 给测试快照补充剩余波次时间、经验、波内击败和总击败数，方便量化战斗压力。
- 连续走完第 1 到第 20 波，逐次验证商店和下一波状态切换。
- 抽查初始武器与六槽传说构筑在前期、中期和首领波的表现。
- 在 390 × 844 手机视口检查选择页和图鉴。

### 实际结果

- 第 1-19 波都准确进入商店，下一波按钮始终只有一个；第 20 波正确结算通关。
- D0 第 1 波约生成 19 个敌人，第 20 波约 128 个；D4 第 20 波约 171 个，压力连续增长。
- 初始普通种子发射器第 1 波 8 秒击败 3 个敌人；直接进入后期时明显乏力，构筑成长是必要的。
- 1 把普通加 5 把传说种子发射器在第 20 波 8 秒击败 27 个敌人，首领仍有 5673 / 5961 生命。
- 手机选择页和图鉴没有横向溢出，内容可以纵向滚动。
- 没有发现阻止 v2.0 发布的玩法或状态机问题。

### 下一步

将 v2.0 合并到 `main`，等待 GitHub Pages 自动部署后进行线上验收。

## 2026-08-21｜v2 第 8 步：正式发布与线上验收

### 发布过程

- 将 `codex/gameplay-parity-v2` 快进合并到 `main`，没有冲突，也没有覆盖远程新提交。
- 推送 `main` 后，GitHub Actions 自动把同一提交同步到 `gh-pages`。
- 核对远程分支，`main` 与 `gh-pages` 均指向发布提交 `72d5e0d`。

### 线上实际验证

- 公开网址显示 `v2.0.0 · 分波战斗与商店构筑`。
- 首次访问自动显示新手指南，关闭后可以选择初始角色与武器。
- 新存档只开放 D0、3 名初始角色，其他危险等级和角色显示正确的锁定状态。
- 选择嫩芽先锋和种子发射器后，线上正式战斗显示 `1 / 20 · D0` 和 20 秒波次倒计时。
- 实际画面中玩家、敌人、树木、弹丸、生命、材料、等级、经验和六个武器槽均正常运行。

### 结论

v2.0 已完成“系统实现 → 本地测试 → 平衡审计 → 20 波回归 → 手机检查 → GitHub Pages 发布 → 线上实玩”的完整开发流程。

## 2026-08-21｜v2.1 第 1 步：从属性差异升级为规则差异

### 为什么继续升级

v2.0 已经有完整循环和足够内容，但多数角色与道具仍主要改变数字。更深的构筑体验需要玩家因为角色规则、道具触发和怪物特性而改变选择，而不是永远购买数值最高的选项。

### 做了什么

- 为全部 12 名探险员加入原创专属特性，包括速度转伤害、静止加护甲、燃烧强化、工程超频、暴击倍率、爆炸范围、额外穿透、速度转攻速、治疗强化和额外材料等。
- 为 8 件核心道具加入条件触发：满生命首击、静止减伤、远距增伤、闪避反击、每 8 次蓄能、概率双倍材料、工程命中维修和闪避治疗。
- 新增硬化、迅捷、巨化、狂怒、晶盾、不稳定 6 种敌人变异，并在敌人头顶显示标记。
- 精英和首领生命条会显示变异名称；图鉴新增“敌人变异”分组。
- 危险等级从 D0-D4 扩展到 D0-D6。D5“深渊”和 D6“终焉”允许双重变异。
- 存档解锁上限改为动态读取危险等级数量，D4 与 D5 通关会依次解锁新难度。
- 增加角色特性、道具特性、有效属性、敌人变异和触发次数的测试快照字段。

### 已完成的专项测试

- 风行侦察员：18 速度实际转化为 6.3 伤害，种子发射器伤害从 13 变成 14。
- 岩壳守卫：静止前 6 护甲，0.8 秒后 10 护甲。
- 玻璃幼芽与风暴电池：12 次攻击触发 5 次道具效果，第 8 次蓄能成功。
- 不稳定敌人：生成 7 个变异标记，击败 3 个后发生 3 次死亡爆炸。
- D6 荆冠斗士同时拥有硬化与晶盾，生命条显示组合名称。
- D4 通关解锁 D5，D5 通关解锁 D6，D6 保持为最高等级。

### 下一步

执行全部角色和核心道具的数据覆盖检查、D6 最终波与手机布局回归，再进行平衡审计和 v2.1 发布。

### 完整回归与平衡结果

- 12 个角色特性、8 个道具触发和 6 个怪物变异都有数据定义与战斗实现，不存在只有文案没有逻辑的条目。
- D6 第 20 波自然生成双变异万巢母体，生命条显示“晶盾+硬化”，危险 D6 通关结算正常。
- 第 1-20 波连续状态机回归通过，19 次“开始下一波”按钮均唯一。
- 四季生长连续触发 20 次，收获从 5 成长到 30，累计获得 300 材料。
- D6 第 20 波生命倍率约 7.81、伤害倍率约 4.28，生成间隔下限仍为 0.26 秒，避免无限制生成拖垮浏览器。
- 390 × 844 手机视口中 7 个危险等级自动分为两行，角色特性文案和主要按钮没有横向溢出。

### 发布判断

当前没有发现阻止 v2.1 发布的脚本、状态机、存档或布局问题。下一步提交独立分支，合并到 `main` 后进行线上验收。
